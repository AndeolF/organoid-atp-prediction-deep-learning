[project]
name = "deeplearningvse-18"
version = "0.1.0"
description = ""
authors = [
    {name = "Andéol"}
]
requires-python = ">=3.10,<3.14"
dependencies = [
    "ipykernel (>=7.2.0,<8.0.0)",
    "matplotlib (>=3.10.8,<4.0.0)",
    "torch (>=2.10.0,<3.0.0)",
    "torchvision (>=0.25.0,<0.26.0)",
    "pandas (<3.0.0)",
    "seaborn (>=0.13.2,<0.14.0)",
    "tqdm (>=4.67.3,<5.0.0)",
    "scikit-learn (<1.8.0)",
    "transformers (>=5.2.0,<6.0.0)",
    "efficientnet-pytorch (==0.6.3)",
    "pretrained-microscopy-models @ git+https://github.com/nasa/pretrained-microscopy-models.git"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
